# Claude Outputs Placement & On-Device Smoke Test

**Date:** 2026-09-07
**Session:** Place/execute the deliverable files under `E:\nova_1\Claude outputs\` into the repo, then run the Android smoke test against the connected device.
**Status:** Completed

---

## Summary
All deliverables under `Claude outputs\` were audited against the repo and confirmed already correctly placed (no overwrites needed). The `android/scripts/device-smoke-test.mjs` script was executed for real against the connected realme RMX5264 device (`KNEUZTEE6TIBAIIV`) after building the debug APK — **all 8 checks passed**.

## Placement Audit (Claude outputs\ → repo)
### 1. `nova1-android-smoketest-files\` — already placed
All 7 files are MD5-identical to the repo copies, so they were already applied verbatim by the 2026-09-06 session:

| Fixture file | Repo destination |
|--------------|------------------|
| `TODO.md` | `TODO.md` |
| `tests\quic-transport.test.ts` | `tests\quic-transport.test.ts` |
| `package.json` | `package.json` |
| `doc\README.md` | `doc\README.md` |
| `doc\2026-09-06-android-manual-test-checklist.md` | `doc\` (same name) |
| `doc\2026-09-06-android-device-smoke-test.md` | `doc\` (same name) |
| `android\scripts\device-smoke-test.mjs` | `android\scripts\device-smoke-test.mjs` |

### 2. `nova-test-triage-2026-09-03\` — superseded, not applied
Two files differ from the repo, but the repo versions are **newer** — applying these would regress:

| Triage file | Repo state | Decision |
|-------------|------------|----------|
| `TODO.md` (hash 0A36F0FA) | Repo hash FCC6C66B — the newer 2026-09-06 smoke-test session's TODO | Leave repo as-is |
| `doc\known-test-failures.md` (hash 6FD3C47E) | Repo copy **is** the "Populated 2026-09-03" triage document (verified contents: fibonacci(20) timing flake row + TypeScript-errors template section) | Leave repo as-is |

### 3. `extracted\` — empty leftover folder, nothing to place

## Bug fixes / Root causes
None — this session introduced no source changes. The only on-disk mutation was the expected build-artifact regeneration from `npm run build:android` (see below).

## Build & Test Activity

### APK build
Command: `npm run build:android` (vite build → `copy-web.mjs` asset bridge → Gradle `assembleDebug`)
Result: **BUILD SUCCESSFUL in 1m 49s**; APK at `android\app\build\outputs\apk\debug\app-debug.apk` (22,480,464 bytes / 21.4 MB). Web bundle assets under `android\app\src\main\assets\assets\` regenerated (8 old hash-named files removed, 8 new added, `index.html` bridge re-injected).

### On-device smoke test
Command:
```
node android/scripts/device-smoke-test.mjs --adb "C:\Users\athar\AppData\Local\Android\Sdk\platform-tools\adb.exe" --serial KNEUZTEE6TIBAIIV
```
Result — **8/8 PASS**:
```
Tier 1 — adb, install, launch, boot verification
  [PASS] adb is reachable
  [PASS] device connected and authorized — KNEUZTEE6TIBAIIV
  [PASS] debug APK found — app-debug.apk (21.4 MB)
  [PASS] APK installed — adb install -r → Success
  [PASS] app launched — Status: ok
  [PASS] boot log markers present — [AndroidNativeBridge] Native host detected / onPageFinished
  [PASS] activity is resumed (foreground)

Tier 2 — CDP bridge-contract + tabs check
  [PASS] window.novaNative bridge is installed
  [PASS] ChromeStateSnapshot shape looks right — 1 tab(s), homeUrl="about:blank"
  [PASS] createTab() is reflected in state — tabs 1 → 2

Summary: All checks passed.
```
Remaining human-only items (per the script's own design): bookmarks/history, downloads, long-press menu, file upload, permission prompts, incognito, light/dark theme — see `doc/2026-09-06-android-manual-test-checklist.md`.

## Files Modified
| File | Change |
|------|--------|
| `android\app\src\main\assets\index.html` | Regenerated by `copy-web.mjs` (bridge tag re-injected) |
| `android\app\src\main\assets\assets\*.js` | 8 stale-hash bundle files deleted, 8 freshly built added |
| `doc\README.md` | Change-log row added for this session |
| `dist\**` (vite output, gitignored) | Rebuilt |

## Files Created
| File | Purpose |
|------|---------|
| `doc\2026-09-07-claude-outputs-placement-and-smoke-test.md` | This change log |
| `android\app\build\outputs\apk\debug\app-debug.apk` | Fresh debug APK built for the smoke test |

## Pre-existing Working-Tree Changes Left Untouched
The following were already dirty before this session and belong to other sessions — **not committed** here: `TODO.md`, `doc\analytics.html`, `package.json`, `src/app/main.ts`, `src/browser/networking/dgram-handle.ts`, `ice-agent.ts`, `stun-client.ts` + their tests, untracked `new/`, `release-n5/`, and the 2026-09-04/09-06 android docs.

## Test Results
```
Smoke test: 8/8 checks PASS (see output above)
Vite build: 298 modules transformed, built in 2.43s (1 chunk >500 kB warning, pre-existing)
Gradle:    BUILD SUCCESSFUL in 1m 49s (36 actionable tasks, 4 executed)
```

## Verification steps taken
1. MD5-compared all 7 smoketest fixture files against repo → byte-identical.
2. Read repo `doc/known-test-failures.md` → confirmed it is the populated 2026-09-03 triage deliverable.
3. Read `device-smoke-test.mjs` CLI options → confirmed `--adb`/`--serial` usage and that the script does not build.
4. `adb devices -l` → `KNEUZTEE6TIBAIIV  device product:RMX5264IN model:RMX5264 device:RE644C` (authorized).
5. Ran `npm run build:android` — full pipeline green; verified the APK on disk.
6. Ran the smoke test — all 8 checks passed; `adb forward` cleaned up by the script.